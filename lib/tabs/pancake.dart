import 'package:flutter/material.dart';

class Pancakes extends StatelessWidget {
  const Pancakes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("pancake"),);
  }
}